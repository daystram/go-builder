stages:
  - generate
  - trigger

generate-config:
  stage: generate
  script: bash generate-jobs.sh
  artifacts:
    paths:
      - configs.yml

trigger-pipelines:
  stage: trigger
  trigger:
    include:
      - artifact: configs.yml
        job: generate-config
    strategy: depend
